export class User {

    constructor(public id: number, public name: string, public lastName: string, public email: string,
    )
    {
        this.id = id;
        this.name = name;
        this.lastName = lastName;
        this.email = email;
    }
}
